const APP = {
    NOT_FOUND: 'Ops, o servidor não consegue atender sua solicitação!'
}

const API = {
    INVALID_PARAMS: 'Request refused: Some requested data was not sent',
    UNKNOWN_ERROR: "An unknow error it happened"
}

module.exports = {
    APP,
    API
}